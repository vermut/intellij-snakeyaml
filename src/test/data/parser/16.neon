---
- debug: msg="test {{ var }}" msg2="test {{ var }}"
list: ["a",b,c]
- item:
    item2: "value {{ var }}"
roles:
    - { role: common, domain: "{{ p_domain | default(omit) }}", dns_nameservers: "{{ p_dns_nameservers | default(omit) }}" }
    - { role: hosts, domain: "{{ p_domain | default(omit) }}" }
